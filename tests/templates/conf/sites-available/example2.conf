# Ensure that Apache listens on port {{ apache_port }}
# Listen {{ apache_port }}
<VirtualHost *:{{ apache_port }}>
    DocumentRoot "{{ apache_document_root }}/example2"
    ServerName example2.com
</VirtualHost>
