# Ensure that Apache listens on port {{ apache_port }}
# Listen {{ apache_port }}
<VirtualHost *:{{ apache_port }}/example1>
    DocumentRoot "{{ apache_document_root }}/example1"
    ServerName example1
</VirtualHost>
